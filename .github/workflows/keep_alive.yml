name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # Cada 10 minutos
  workflow_dispatch:  # También se puede ejecutar manualmente

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: |
          curl -s https://padelpro-tfg.onrender.com/api/usuarios/login || true
          echo "Ping enviado a Render"